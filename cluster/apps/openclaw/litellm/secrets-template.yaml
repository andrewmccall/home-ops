---
apiVersion: v1
kind: Secret
metadata:
  name: litellm-secrets
  namespace: openclaw
stringData:
  gateway-token: "REPLACE_WITH_YOUR_GATEWAY_TOKEN"
  litellm-key: "REPLACE_WITH_RANDOM_STRING"  # HA uses this as the OpenAI API key
