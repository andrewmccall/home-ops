# Phase 01.1: HA OpenClaw Shared Session Bridge - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-13
**Phase:** 01.1-ha-openclaw-shared-session-bridge
**Areas discussed:** Bridge contract, Shared identity policy, Failure behavior, LiteLLM relationship

---

## Bridge contract

| Option | Description | Selected |
|--------|-------------|----------|
| OpenAI-compatible drop-in endpoint for the HA config-entry | Keep the built-in Home Assistant config-entry flow and hide OpenClaw/session specifics behind the bridge | ✓ |
| More custom HA-specific shim | Accept extra HA-side work or customization to reach the bridge | |

**User's choice:** OpenAI-compatible drop-in endpoint for the HA config-entry.
**Notes:** The bridge should look like a normal OpenAI-style backend to Home Assistant so the UI/config-entry flow remains the operator surface.

---

## Shared identity policy

| Option | Description | Selected |
|--------|-------------|----------|
| One durable Andrew/Ada shared identity across HA voice and Telegram | Preserve true shared continuity across channels | ✓ |
| Stable per-device or per-assistant identity | Keep continuity only within a narrower HA-local session boundary | |

**User's choice:** One durable Andrew/Ada shared identity across HA voice and Telegram.
**Notes:** This phase should restore the original shared Ada contract instead of creating a separate Home Assistant session namespace.

---

## Failure behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Fail closed with a clear error | Refuse to answer when continuity cannot be guaranteed | |
| Fall back to a stateless/shared-token path | Keep voice available even if true continuity is temporarily unavailable | ✓ |

**User's choice:** Fall back to a stateless/shared-token path.
**Notes:** Follow-up clarification locked that degraded continuity must be visible, not silent.

---

## Degraded-mode signaling

| Option | Description | Selected |
|--------|-------------|----------|
| Surface a clear degraded-mode signal | Make continuity loss visible to the user or operator | ✓ |
| Keep it seamless and silent | Prioritize uninterrupted voice UX even if the session is no longer shared | |

**User's choice:** Surface a clear degraded-mode signal.
**Notes:** Silent split-brain behavior is not acceptable.

---

## LiteLLM relationship

| Option | Description | Selected |
|--------|-------------|----------|
| Keep the HA bridge separate and leave LiteLLM in place | Avoid expanding this phase into proxy cleanup/removal | |
| Replace LiteLLM for HA voice immediately | Use the new bridge as the new proxy surface for this path | ✓ |

**User's choice:** Replace LiteLLM for HA voice immediately.
**Notes:** Follow-up clarification locked a stronger cutover goal: remove LiteLLM entirely if the bridge fully covers its current job.

---

## LiteLLM removal scope

| Option | Description | Selected |
|--------|-------------|----------|
| Remove LiteLLM entirely if the bridge covers its current job | Avoid duplicate proxy layers and simplify the stack | ✓ |
| Stop using LiteLLM for HA voice only | Leave LiteLLM deployed for other callers | |

**User's choice:** Remove LiteLLM entirely if the bridge covers its current job.
**Notes:** Planning should include verifying coverage before removal.

## the agent's Discretion

- Exact continuity metadata field and request-shaping details toward OpenClaw.
- Exact in-cluster runtime and manifest structure for the bridge.
- Exact degraded-mode response wording and health-check design.

## Deferred Ideas

- Multi-user or per-device continuity models after the first shared-identity bridge works.
- A custom Home Assistant integration or fork path if the drop-in bridge approach later proves insufficient.
