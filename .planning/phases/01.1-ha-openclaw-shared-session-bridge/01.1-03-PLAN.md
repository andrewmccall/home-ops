---
phase: 01.1-ha-openclaw-shared-session-bridge
plan: "03"
type: execute
wave: 3
depends_on: ["01.1-01", "01.1-02"]
files_modified:
  - cluster/apps/openclaw/instance/ha-config-snippet.yaml
  - cluster/apps/house/home-assistant/home-assistant.yaml
  - .planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md
  - .planning/phases/01.1-ha-openclaw-shared-session-bridge/CUTOVER-CHECKLIST.md
autonomous: false
requirements: [VOICE-03, VOICE-04]
user_setup:
  - service: home-assistant
    why: "The built-in OpenAI Conversation integration must still be updated through the supported UI/config-entry path on this deployment"
    dashboard_config:
      - task: "Update or recreate the OpenAI Conversation config entry to point at the bridge base URL, bridge API key, and model; then bind it to the Ada voice assistant"
        location: "Home Assistant Settings -> Devices & Services and Settings -> Voice Assistants"
must_haves:
  truths:
    - "Home Assistant can switch to the bridge through the supported UI path without a custom integration."
    - "Home Assistant voice and Telegram can be manually verified against one shared Ada conversation."
    - "The HA spoof path resolves to the intended bridge route before live cutover."
  artifacts:
    - path: cluster/apps/openclaw/instance/ha-config-snippet.yaml
      provides: "Updated operator instructions for bridge-based config-entry setup"
    - path: cluster/apps/house/home-assistant/home-assistant.yaml
      provides: "Correct api.openai.com host alias for the bridge ingress path"
    - path: .planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md
      provides: "Manual continuity and degraded-mode checklist"
    - path: .planning/phases/01.1-ha-openclaw-shared-session-bridge/CUTOVER-CHECKLIST.md
      provides: "Ingress/TLS and HA cutover checklist before LiteLLM removal"
  key_links:
    - from: cluster/apps/house/home-assistant/home-assistant.yaml
      to: api.openai.com
      via: hostAliases
      pattern: "hostAliases:"
    - from: cluster/apps/openclaw/instance/ha-config-snippet.yaml
      to: bridge base URL/API key/model
      via: config-entry notes
      pattern: "Base URL:"
    - from: MANUAL-TEST.md
      to: Telegram + HA voice
      via: cross-channel verification steps
      pattern: "Telegram"
---

<objective>
Prepare and validate the supported Home Assistant cutover to the bridge.

Purpose: D-02 requires keeping the normal Home Assistant UI/config-entry flow, and VOICE-04 requires a live cross-channel continuity check before cleanup.
Output: Updated HA setup docs, hostAlias alignment, and a manual cutover/verification checklist.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-CONTEXT.md
@.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-RESEARCH.md
@cluster/apps/openclaw/instance/ha-config-snippet.yaml
@cluster/apps/house/home-assistant/home-assistant.yaml

<interfaces>
Current HA deployment behavior:
- `home-assistant.yaml` hostAliases currently maps `api.openai.com` to an internal ingress IP.
- HA config-entry must still use Base URL + API key + model, not a custom integration.
- Manual proof of VOICE-04 is: teach Ada something in Telegram, ask from HA voice, verify recall.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Home Assistant bridge cutover docs and align the spoof-path host alias</name>
  <read_first>
    cluster/apps/openclaw/instance/ha-config-snippet.yaml
    cluster/apps/house/home-assistant/home-assistant.yaml
    cluster/apps/openclaw/bridge/openai-proxy-ingress.yaml
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-RESEARCH.md
  </read_first>
  <files>
    cluster/apps/openclaw/instance/ha-config-snippet.yaml
    cluster/apps/house/home-assistant/home-assistant.yaml
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/CUTOVER-CHECKLIST.md
  </files>
  <action>
    Rewrite the HA config snippet for the bridge path per D-01/D-02: the supported UI flow should still ask for Base URL, API key, and model, but now target the bridge and dedicated bridge key instead of direct OpenClaw/shared token notes. Add a manual test guide covering Ada naming, Telegram↔HA continuity per D-03/D-04, explicit degraded-mode behavior per D-06, and denial behavior. Create `.planning/phases/01.1-ha-openclaw-shared-session-bridge/CUTOVER-CHECKLIST.md` with the required pre-cutover checks for ingress/TLS ownership transfer, hostAlias/IP alignment, bridge secret/config readiness, HA config-entry update, and post-cutover approval gating before LiteLLM removal. Use the live bridge ingress status as the source of truth for the intended spoof-path IP: `kubectl get ingress -n openclaw openai-proxy -o jsonpath='{.status.loadBalancer.ingress[0].ip}'`. Before editing `home-assistant.yaml`, compare the repo’s `api.openai.com` hostAlias IP to that command output; if mismatched, update the hostAlias so `api.openai.com` resolves to the live bridge ingress target. Do not introduce a custom HA integration or YAML conversation config.
  </action>
  <verify>
    <automated>BRIDGE_IP="$(kubectl get ingress -n openclaw openai-proxy -o jsonpath='{.status.loadBalancer.ingress[0].ip}')" && grep -n "Base URL:\|API key:\|Model:\|Ada" cluster/apps/openclaw/instance/ha-config-snippet.yaml && grep -q "\"${BRIDGE_IP}\"" cluster/apps/house/home-assistant/home-assistant.yaml && grep -n "Telegram\|degraded\|denial" .planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md && grep -n "hostAlias\|ingress\|TLS\|approved" .planning/phases/01.1-ha-openclaw-shared-session-bridge/CUTOVER-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    The setup notes describe the bridge-based UI flow, the manual checklist covers continuity/degraded-mode/denial checks, the cutover checklist covers ingress/TLS and approval gating, and the Home Assistant manifest no longer points `api.openai.com` at a stale ingress IP.
  </acceptance_criteria>
  <done>Operators have exact cutover instructions, the HA manifest matches the intended bridge ingress path, and bridge cleanup has a formal approval checklist.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Perform Home Assistant UI cutover and approve the shared-session bridge</name>
  <read_first>
    cluster/apps/openclaw/instance/ha-config-snippet.yaml
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/CUTOVER-CHECKLIST.md
  </read_first>
  <action>
    Perform the Home Assistant UI/config-entry cutover to the bridge and approve the bridge path for post-cutover cleanup only after the manual continuity, degraded-mode, and denial checks pass.
  </action>
  <what-built>Bridge deployment, bridge smoke script, updated Home Assistant cutover instructions, and manual verification checklist.</what-built>
  <instructions>
    1. In Home Assistant, update or recreate the OpenAI Conversation config entry using the bridge Base URL, bridge API key, and model from the updated snippet.
    2. Bind that config entry to the Ada voice assistant.
    3. Confirm the pre-cutover items in `CUTOVER-CHECKLIST.md` are satisfied, including ingress/TLS ownership and hostAlias alignment.
    3a. If you need to prove degraded mode deterministically, temporarily enable the bridge-side test seam `BRIDGE_FORCE_DEGRADED_MODE=true`, restart the bridge, then continue with the degraded-mode check below.
    4. Run the manual checklist:
       - ask Ada her name by HA voice and confirm consistent Ada identity
       - teach Ada a fact in Telegram, then ask via HA voice and confirm recall
       - ask a blocked admin/destructive action and confirm clear denial
       - force or observe degraded mode using the documented `BRIDGE_FORCE_DEGRADED_MODE=true` test seam if needed, and confirm the user still receives a response and that the degraded state is explicit, not silent
    5. Approve cleanup only if the shared-session bridge passes those checks.
  </instructions>
  <verification>
    3. Run the manual checklist:
       - ask Ada her name by HA voice and confirm consistent Ada identity
       - teach Ada a fact in Telegram, then ask via HA voice and confirm recall
       - ask a blocked admin/destructive action and confirm clear denial
       - force or observe degraded mode using the documented `BRIDGE_FORCE_DEGRADED_MODE=true` test seam if needed, and confirm the user still receives a response and that the degraded state is explicit, not silent
  </verification>
  <resume-signal>Reply with “approved” after successful cutover, or describe the exact failing step.</resume-signal>
  <verify>
    <automated>grep -n "OpenAI Conversation\|Voice Assistants\|Telegram\|degraded" .planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md</automated>
  </verify>
  <acceptance_criteria>
    Human verification confirms the supported UI path works, Ada identity remains consistent, Telegram and HA share one conversation, and degraded mode is visibly surfaced when continuity cannot be preserved.
  </acceptance_criteria>
  <done>Home Assistant is live on the bridge path and approved for post-cutover cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator → Home Assistant UI | Human applies the supported config-entry cutover |
| Home Assistant pod → internal spoof path | Host alias and internal ingress route control where HA actually sends OpenAI traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01.1-07 | S | Home Assistant config entry | mitigate | Keep the normal UI path and document exact bridge Base URL/API key/model values to avoid accidental direct-token usage |
| T-01.1-08 | T | `home-assistant.yaml` hostAliases | mitigate | Validate and correct the `api.openai.com` target IP before cutover |
| T-01.1-09 | R | Manual continuity approval | mitigate | Require explicit human approval after the Telegram↔HA continuity and degraded-mode checks |
</threat_model>

<verification>
Repo checks must pass first; then the human cutover checklist must complete with an explicit approval signal.
</verification>

<success_criteria>
- Operators can follow the supported HA UI flow without a custom component.
- HA voice proves shared continuity with Telegram.
- Degraded continuity is visible to the user.
- This plan produces the gate for LiteLLM cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-03-SUMMARY.md`
</output>
