---
phase: 01.1-ha-openclaw-shared-session-bridge
plan: "04"
type: execute
wave: 4
depends_on: ["01.1-02", "01.1-03"]
files_modified:
  - cluster/apps/openclaw/kustomization.yaml
  - cluster/apps/openclaw/litellm/kustomization.yaml
  - cluster/apps/openclaw/litellm/litellm.yaml
  - cluster/apps/openclaw/litellm/litellm-secrets.sops.yaml
  - cluster/apps/openclaw/litellm/openai-proxy-ingress.yaml
  - cluster/apps/openclaw/litellm/openai-proxy-tls-secret.yaml
autonomous: true
requirements: [VOICE-03, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "After LiteLLM cleanup, Home Assistant still reaches one working Ada bridge path."
    - "After LiteLLM removal, HA voice still reaches Ada identity, full context, and denial behavior."
    - "After cleanup, the HA spoof path has one clear owner and no competing proxy layer."
  artifacts:
    - path: cluster/apps/openclaw/kustomization.yaml
      provides: "Bridge retained, LiteLLM removed from top-level deployment"
    - path: cluster/apps/openclaw/litellm/kustomization.yaml
      provides: "Residual LiteLLM resources removed or emptied for cleanup"
  key_links:
    - from: cluster/apps/openclaw/kustomization.yaml
      to: cluster/apps/openclaw/bridge/kustomization.yaml
      via: resource include
      pattern: "bridge"
    - from: cluster/apps/openclaw/kustomization.yaml
      to: litellm
      via: resource removal
      pattern: "litellm"
---

<objective>
Remove LiteLLM only after successful bridge cutover and ingress/TLS ownership migration.

Purpose: D-07 requires cleanup when the bridge fully replaces the proxy role, but only after the safer cutover gate completes.
Output: LiteLLM manifests removed from active deployment and duplicate spoof-path ownership eliminated.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-CONTEXT.md
@.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-RESEARCH.md
@cluster/apps/openclaw/kustomization.yaml
@cluster/apps/openclaw/litellm/kustomization.yaml
@cluster/apps/openclaw/litellm/litellm.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove LiteLLM from active OpenClaw deployment after bridge approval</name>
  <read_first>
    cluster/apps/openclaw/kustomization.yaml
    cluster/apps/openclaw/litellm/kustomization.yaml
    cluster/apps/openclaw/litellm/litellm.yaml
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-RESEARCH.md
  </read_first>
  <files>
    cluster/apps/openclaw/kustomization.yaml
    cluster/apps/openclaw/litellm/kustomization.yaml
    cluster/apps/openclaw/litellm/litellm.yaml
    cluster/apps/openclaw/litellm/litellm-secrets.sops.yaml
    cluster/apps/openclaw/litellm/openai-proxy-ingress.yaml
    cluster/apps/openclaw/litellm/openai-proxy-tls-secret.yaml
  </files>
  <action>
    After Plan 03 approval, remove LiteLLM from the active top-level OpenClaw kustomization and delete or retire the now-obsolete LiteLLM manifests, including the old ingress/TLS copies that were moved under bridge ownership in Plan 02. Keep the new bridge package as the sole HA-facing proxy layer. Do not touch the bridge-owned ingress/TLS files or the OpenClaw safety prompt. This cleanup must preserve VOICE-03, SAFE-02, and SAFE-03 by relying on the bridge + OpenClaw path that was already validated.
  </action>
  <verify>
    <automated>! grep -q "litellm" cluster/apps/openclaw/kustomization.yaml && ! grep -q "openai-proxy-ingress.yaml" cluster/apps/openclaw/litellm/kustomization.yaml && ! grep -q "openai-proxy-tls-secret.yaml" cluster/apps/openclaw/litellm/kustomization.yaml</automated>
  </verify>
  <acceptance_criteria>
    The top-level OpenClaw deployment no longer includes LiteLLM, duplicate spoof-path resources are removed from the LiteLLM package, and the bridge remains the single HA-facing proxy owner.
  </acceptance_criteria>
  <done>LiteLLM is no longer deployed in parallel once bridge cutover is approved.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run bridge-focused repo checks after cleanup</name>
  <read_first>
    scripts/test-ha-openclaw-bridge.sh
    cluster/apps/openclaw/kustomization.yaml
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md
  </read_first>
  <files>cluster/apps/openclaw/kustomization.yaml</files>
  <action>
    Use the bridge smoke harness as the post-cleanup verification reference and ensure the repo still points HA traffic at the bridge-only path. Run the smoke harness against a port-forwarded bridge service so executor-side DNS/TLS assumptions do not matter, then rerun the Telegram↔HA shared-session step from `MANUAL-TEST.md` as a post-cleanup VOICE-04 spot-check before declaring the phase complete. This task is verification-focused: do not reintroduce LiteLLM or revert ingress ownership.
  </action>
  <verify>
    <automated>grep -n "bridge" cluster/apps/openclaw/kustomization.yaml && ! grep -q "litellm" cluster/apps/openclaw/kustomization.yaml && bash -n scripts/test-ha-openclaw-bridge.sh && kubectl port-forward -n openclaw svc/openclaw-bridge 18081:80 >/dev/null 2>&1 & PF_PID=$!; trap 'kill $PF_PID' EXIT; sleep 5; scripts/test-ha-openclaw-bridge.sh "http://127.0.0.1:18081/v1" "${HA_OPENCLAW_BRIDGE_TOKEN}"; kill $PF_PID; trap - EXIT && grep -n "Telegram" .planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md</automated>
  </verify>
  <acceptance_criteria>
    The OpenClaw top-level kustomization still includes the bridge, no LiteLLM include remains in the active path, the bridge smoke script executes successfully against the deployed bridge service after cleanup, and the post-cleanup checklist explicitly re-runs the Telegram↔HA continuity spot-check before phase completion.
  </acceptance_criteria>
  <done>Cleanup is complete and the bridge remains the sole validated proxy path with behavior and cross-channel continuity rechecked after LiteLLM removal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| top-level deployment selection → runtime traffic path | Kustomize ownership determines whether HA reaches bridge or stale proxy layers |
| retired LiteLLM assets → live bridge assets | Duplicate ownership can reintroduce conflicting ingress/TLS behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01.1-10 | T | top-level `cluster/apps/openclaw/kustomization.yaml` | mitigate | Remove `litellm` only after Plan 03 approval; keep `bridge` as the single active proxy layer |
| T-01.1-11 | D | duplicate ingress/TLS resources | mitigate | Delete or retire old LiteLLM-owned spoof assets after they are moved to bridge ownership |
| T-01.1-12 | R | post-cleanup verification | mitigate | Reuse the bridge-specific smoke harness so cleanup does not silently regress validated behavior |
</threat_model>

<verification>
Confirm no active LiteLLM include remains and the bridge smoke script is still valid after cleanup.
</verification>

<success_criteria>
- LiteLLM is removed only after bridge approval.
- Duplicate ingress/TLS ownership is eliminated.
- The bridge remains the sole HA-facing proxy path.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-04-SUMMARY.md`
</output>
