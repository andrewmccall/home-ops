---
phase: 01.1-ha-openclaw-shared-session-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - cluster/apps/openclaw/instance/openclaw-instance.yaml
  - scripts/test-ha-openclaw-bridge.sh
autonomous: true
requirements: [SAFE-02, SAFE-03]
must_haves:
  truths:
    - "The HA bridge path can rely on OpenClaw for the exact upstream Responses API surface Home Assistant expects."
    - "The bridge smoke harness proves identity, context, denial, degraded-mode signaling, and streaming behavior against the HA-compatible surface."
  artifacts:
    - path: cluster/apps/openclaw/instance/openclaw-instance.yaml
      provides: "Responses endpoint enabled upstream"
    - path: scripts/test-ha-openclaw-bridge.sh
      provides: "Bridge-specific smoke coverage for compatibility and guardrails"
  key_links:
    - from: scripts/test-ha-openclaw-bridge.sh
      to: /v1/models
      via: curl
      pattern: "/v1/models"
    - from: scripts/test-ha-openclaw-bridge.sh
      to: /v1/responses
      via: curl
      pattern: "/v1/responses"
    - from: cluster/apps/openclaw/instance/openclaw-instance.yaml
      to: OpenClaw gateway
      via: gateway.http.endpoints.responses
      pattern: "responses:"
---

<objective>
Enable the upstream OpenClaw capabilities and validation harness the new Home Assistant bridge depends on.

Purpose: Home Assistant 2026.3.4 now validates with `/v1/models` and talks through `/v1/responses`, so this plan removes the old chat-completions-only mismatch before bridge cutover.
Output: Responses support enabled in OpenClaw plus a bridge-specific smoke script.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-CONTEXT.md
@.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-RESEARCH.md
@cluster/apps/openclaw/instance/openclaw-instance.yaml
@scripts/test-ha-voice-token.sh

<interfaces>
- Home Assistant now requires `GET /v1/models` and `POST /v1/responses`.
- OpenClaw continuity metadata must be injected with `x-openclaw-session-key` and `x-openclaw-message-channel`.
- Safety denials stay in Ada/OpenClaw policy, not in a narrowed bridge policy layer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bridge smoke script for compatibility, context, denial, and degraded-mode visibility</name>
  <read_first>
    scripts/test-ha-voice-token.sh
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-RESEARCH.md
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-VALIDATION.md
  </read_first>
  <files>scripts/test-ha-openclaw-bridge.sh</files>
  <behavior>
    - Test 1: `GET /v1/models` with the bridge key returns a model list including the configured model.
    - Test 2: `POST /v1/responses` returns an Ada-identity answer through the bridge path.
    - Test 3: A destructive/admin request is denied clearly, preserving SAFE-03.
    - Test 4: A memory/tools prompt shows SAFE-02 still reaches full Ada context.
    - Test 5: When the bridge is run with `BRIDGE_FORCE_DEGRADED_MODE=true`, continuity injection is skipped but the request still returns a successful degraded response instead of hard-failing the request, per D-05.
    - Test 6: `POST /v1/responses` with `"stream": true` proves the bridge path preserves streaming-compatible behavior instead of downgrading to a non-streaming-only response.
  </behavior>
  <action>
    Create a new smoke script for the researched HA-compatible surface, using `/v1/models` and `/v1/responses` instead of `/chat/completions`. Reuse the repo’s shell + curl style, parse JSON with Python like the existing script, and make the checks grep-verifiable. Include an explicit streaming `/v1/responses` probe so the script confirms the bridge path preserves HA-compatible streaming semantics rather than buffering everything into a non-streaming-only shape. Add a degraded-fallback probe that uses the documented bridge-side test seam `BRIDGE_FORCE_DEGRADED_MODE=true` and asserts the request still succeeds with a clearly marked degraded response instead of hard-failing or silently opening a separate session. Keep the script bridge-focused; do not add legacy chat-completions fallback assertions.
  </action>
  <verify>
    <automated>bash -n scripts/test-ha-openclaw-bridge.sh && grep -n "/v1/models\|/v1/responses\|stream\|degraded" scripts/test-ha-openclaw-bridge.sh</automated>
  </verify>
  <acceptance_criteria>
    `scripts/test-ha-openclaw-bridge.sh` exists, is shell-valid, targets `/v1/models` and `/v1/responses`, includes a streaming `/v1/responses` check, and includes explicit checks for Ada identity, denial behavior, full-context behavior, and successful degraded fallback with visible signaling.
  </acceptance_criteria>
  <done>Bridge smoke coverage exists for SAFE-02 and SAFE-03 on the correct HA-compatible API surface, including streaming behavior and successful degraded fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Enable native OpenClaw responses support for the bridge path</name>
  <read_first>
    cluster/apps/openclaw/instance/openclaw-instance.yaml
    .planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-RESEARCH.md
  </read_first>
  <files>cluster/apps/openclaw/instance/openclaw-instance.yaml</files>
  <action>
    Update the OpenClaw instance raw gateway config to enable the Responses endpoint alongside the existing chat-completions support so the bridge can passthrough native `/v1/responses` requests. Preserve the existing Ada prompt override and refusal behavior so SAFE-03 remains enforced in Ada/OpenClaw policy rather than being reimplemented in the bridge. Do not remove chat completions in this phase.
  </action>
  <verify>
    <automated>grep -n "chatCompletions:\|responses:" cluster/apps/openclaw/instance/openclaw-instance.yaml</automated>
  </verify>
  <acceptance_criteria>
    `openclaw-instance.yaml` contains an enabled `gateway.http.endpoints.responses` block while retaining the existing Ada systemPromptOverride safety rules and current chat-completions support.
  </acceptance_criteria>
  <done>OpenClaw is configured to accept the HA-required upstream Responses traffic without dropping existing protections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| bridge smoke client → bridge API | Untrusted request/response assertions cross here |
| bridge → OpenClaw gateway | Shared continuity headers and upstream token cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01.1-01 | T | `scripts/test-ha-openclaw-bridge.sh` | mitigate | Assert exact HA-compatible endpoints and explicit degraded markers so silent protocol drift is caught quickly |
| T-01.1-02 | I | `openclaw-instance.yaml` | mitigate | Keep Ada safety prompt in OpenClaw config; do not bypass policy in the bridge |
| T-01.1-03 | D | OpenClaw gateway endpoints | mitigate | Enable `/v1/responses` without removing current endpoints so cutover is additive, not breaking |
</threat_model>

<verification>
Run `bash -n scripts/test-ha-openclaw-bridge.sh` and grep the OpenClaw manifest for `responses:` before moving to bridge deployment.
</verification>

<success_criteria>
- OpenClaw manifest exposes responses support.
- A dedicated smoke script exists for `/v1/models` + `/v1/responses`.
- SAFE-02 and SAFE-03 are testable on the new bridge path.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-01-SUMMARY.md`
</output>
