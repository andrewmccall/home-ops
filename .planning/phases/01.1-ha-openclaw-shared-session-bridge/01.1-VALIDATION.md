---
phase: 01.1
slug: ha-openclaw-shared-session-bridge
status: ready
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-13
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | shell smoke tests + manual Home Assistant verification |
| **Config file** | none |
| **Quick run command** | `kubectl port-forward -n openclaw svc/<bridge-service> 18080:<port> && curl -sS http://127.0.0.1:18080/v1/models -H 'Authorization: Bearer <bridge-key>'` |
| **Full suite command** | bridge smoke script + manual end-to-end HA voice verification |
| **Estimated runtime** | ~60 seconds plus manual HA checks |

---

## Sampling Rate

- **After every task commit:** Run `kubectl port-forward -n openclaw svc/<bridge-service> 18080:<port> && curl -sS http://127.0.0.1:18080/v1/models -H 'Authorization: Bearer <bridge-key>'`
- **After every plan wave:** Run `bridge smoke script + manual end-to-end HA voice verification`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds for smoke feedback

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 1 | SAFE-02 / SAFE-03 | T-01.1-01 | Upstream OpenClaw and the bridge smoke harness prove the correct API surface, streaming compatibility, context reach, denials, and successful degraded fallback signaling | smoke | `bash -n scripts/test-ha-openclaw-bridge.sh && grep -n "responses:" cluster/apps/openclaw/instance/openclaw-instance.yaml && grep -n "stream\\|fallback\\|degraded" scripts/test-ha-openclaw-bridge.sh` | ❌ W0 | ⬜ pending |
| 01.1-02-01 | 02 | 2 | VOICE-03 / VOICE-04 / SAFE-02 | T-01.1-04 | The bridge exposes HA-compatible `/v1/models` and `/v1/responses`, preserves streaming behavior, injects deterministic continuity metadata, performs a successful visible degraded fallback when needed, and transfers ingress/TLS ownership without duplicate live resources | smoke | `grep -R -n "/v1/models\\|/v1/responses\\|stream\\|x-openclaw-session-key\\|degraded\\|fallback\\|BRIDGE_FORCE_DEGRADED_MODE" cluster/apps/openclaw/bridge && grep -n "bridge-runtime-configmap" cluster/apps/openclaw/bridge/bridge.yaml && ! grep -q "openai-proxy-ingress.yaml" cluster/apps/openclaw/litellm/kustomization.yaml && ! grep -q "openai-proxy-tls-secret.yaml" cluster/apps/openclaw/litellm/kustomization.yaml && kubectl port-forward -n openclaw svc/openclaw-bridge 18080:80 >/dev/null 2>&1 & PF_PID=$!; trap 'kill $PF_PID' EXIT; sleep 5; scripts/test-ha-openclaw-bridge.sh "http://127.0.0.1:18080/v1" "${HA_OPENCLAW_BRIDGE_TOKEN}"; kill $PF_PID; trap - EXIT` | ❌ W0 | ⬜ pending |
| 01.1-03-01 | 03 | 3 | VOICE-03 / VOICE-04 | T-01.1-07 | HA cutover follows the supported UI path and has explicit continuity/degraded-mode approval gates | manual | `grep -n "Telegram\\|degraded\\|denial" .planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md && grep -n "hostAlias\\|ingress\\|TLS\\|approved" .planning/phases/01.1-ha-openclaw-shared-session-bridge/CUTOVER-CHECKLIST.md` | ❌ W0 | ⬜ pending |
| 01.1-04-01 | 04 | 4 | VOICE-03 / SAFE-02 / SAFE-03 / VOICE-04 | T-01.1-10 | LiteLLM cleanup leaves one working HA-facing proxy path with no regression in Ada behavior or cross-channel continuity | smoke + manual | `grep -n "bridge" cluster/apps/openclaw/kustomization.yaml && ! grep -q "litellm" cluster/apps/openclaw/kustomization.yaml && bash -n scripts/test-ha-openclaw-bridge.sh && kubectl port-forward -n openclaw svc/openclaw-bridge 18081:80 >/dev/null 2>&1 & PF_PID=$!; trap 'kill $PF_PID' EXIT; sleep 5; scripts/test-ha-openclaw-bridge.sh "http://127.0.0.1:18081/v1" "${HA_OPENCLAW_BRIDGE_TOKEN}"; kill $PF_PID; trap - EXIT` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `scripts/test-ha-openclaw-bridge.sh` — bridge smoke tests for `/v1/models`, `/v1/responses`, streaming, identity, denial, context, and successful degraded fallback
- [ ] `.planning/phases/01.1-ha-openclaw-shared-session-bridge/MANUAL-TEST.md` — HA config-entry update and continuity verification steps
- [ ] `.planning/phases/01.1-ha-openclaw-shared-session-bridge/CUTOVER-CHECKLIST.md` — ingress/TLS cutover checklist for moving assets out of `cluster/apps/openclaw/litellm/`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Telegram and Home Assistant share one Ada session | VOICE-04 | Requires cross-channel live interaction | Tell Ada something in Telegram, ask via HA voice, verify recall |
| Degraded mode is clearly surfaced when continuity is unavailable | VOICE-04 | Needs live user-facing behavior review | Force degraded bridge path and confirm the reply makes degradation clear |
| HA config-entry still works through the supported UI path | VOICE-03 | UI/config-entry flow is product behavior, not pure API behavior | Recreate or update the OpenAI Conversation config entry and verify the assistant stays bound to Ada |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all missing references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s for smoke checks
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
