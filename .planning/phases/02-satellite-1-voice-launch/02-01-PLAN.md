---
phase: 02-satellite-1-voice-launch
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - cluster/apps/house/wyoming-openwakeword/kustomization.yaml
  - cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml
  - cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml
  - cluster/apps/house/wyoming-openwakeword/models/MODEL.md
  - scripts/voice/render-wakeword-configmap.sh
  - scripts/voice/smoke-satellite1-runtime.sh
  - scripts/voice/check-wakeword-rollout.sh
autonomous: true
requirements:
  - WAKE-02
must_haves:
  truths:
    - "Operator has a dedicated wake-word release definition that is separate from Home Assistant, Whisper, and Piper."
    - "Operator can render externally generated Ada wake-word artifacts into one Kubernetes ConfigMap without hand-editing the HelmRelease."
    - "Operator can verify wake-word runtime health and prove future model refreshes only touch the wake-word package."
  artifacts:
    - path: cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml
      provides: "Dedicated Wyoming openWakeWord HelmRelease on port 10400 with a mounted model directory"
    - path: cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml
      provides: "Named ConfigMap shell for ada.tflite and hey_ada.tflite model payloads"
    - path: scripts/voice/render-wakeword-configmap.sh
      provides: "Deterministic render step from checked-in .tflite files to wakeword-models.yaml"
    - path: scripts/voice/check-wakeword-rollout.sh
      provides: "Blast-radius verification for model-only wake-word updates"
  key_links:
    - from: scripts/voice/render-wakeword-configmap.sh
      to: cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml
      via: "generated ConfigMap manifest"
      pattern: "wakeword-models"
    - from: cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml
      to: cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml
      via: "ConfigMap mount at /models"
      pattern: "custom-model-dir|/models|wakeword-models"
---

<objective>
Prepare the dedicated Satellite 1 wake-word package and operator tooling before any real Ada model artifacts are introduced.

Purpose: WAKE-02 requires model deployment and updates to be independent from the existing Home Assistant, Whisper, Piper, and bridge path.
Output: A repo-native wake-word release skeleton plus deterministic render and rollout-check scripts.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-satellite-1-voice-launch/02-CONTEXT.md
@.planning/phases/02-satellite-1-voice-launch/02-RESEARCH.md
@cluster/apps/house/home-assistant/home-assistant.yaml
@cluster/apps/house/kustomization.yaml
@.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-02-SUMMARY.md

<decisions>
D-01: Satellite 1 is one Assist-compatible device with both mic and speaker.
D-02: Replies return on the same Satellite 1 device.
D-03: Do not design this phase around split endpoints or multiple endpoint classes.
D-04: Custom "Ada" / "Hey Ada" wake word is the primary launch path.
D-05: Manual Assist stays available as fallback.
D-06: Model training stays external; packaging, deployment, and updates happen in-repo and in-cluster.
D-07: Preserve the existing HA -> bridge -> OpenClaw -> Piper path.
D-08: Failures must be operator-visible, not silent.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the dedicated Wyoming openWakeWord release without activating it yet</name>
  <read_first>
    - .planning/phases/02-satellite-1-voice-launch/02-CONTEXT.md
    - .planning/phases/02-satellite-1-voice-launch/02-RESEARCH.md
    - cluster/apps/house/home-assistant/home-assistant.yaml
    - cluster/apps/house/kustomization.yaml
  </read_first>
  <files>
    - cluster/apps/house/wyoming-openwakeword/kustomization.yaml
    - cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml
    - cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml
  </files>
  <action>Create `cluster/apps/house/wyoming-openwakeword/` as a separate bjw-s `app-template` release using `rhasspy/wyoming-openwakeword:2.1.0` pinned to a digest, listening on TCP `10400`, with `--uri tcp://0.0.0.0:10400 --custom-model-dir /models`, a TCP readiness/liveness probe, and a ConfigMap-backed `/models` mount named `wakeword-models`. Keep this release out of `cluster/apps/house/kustomization.yaml` in this plan so Flux does not reconcile it before real `ada.tflite` / `hey_ada.tflite` artifacts exist (per D-04, D-06). Do not edit `cluster/apps/house/home-assistant/home-assistant.yaml` and do not introduce any new conversation backend path (per D-07). Define `wakeword-models.yaml` with the final resource name and the exact binary keys `ada.tflite` and `hey_ada.tflite` so Plan 02 only replaces payload values, not manifest structure.</action>
  <verify>
    <automated>grep -q "rhasspy/wyoming-openwakeword" cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml && grep -q "10400" cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml && grep -q "wakeword-models" cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml && grep -q "ada.tflite" cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml && grep -q "hey_ada.tflite" cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml && ! grep -q "wyoming-openwakeword" cluster/apps/house/kustomization.yaml</automated>
  </verify>
  <acceptance_criteria>
    - A dedicated wake-word release exists under `cluster/apps/house/wyoming-openwakeword/`.
    - The release mounts `wakeword-models` at `/models` and serves Wyoming traffic on `10400`.
    - `cluster/apps/house/home-assistant/home-assistant.yaml` remains unchanged in this plan.
    - `cluster/apps/house/kustomization.yaml` does not yet activate the wake-word release.
  </acceptance_criteria>
  <done>The repo contains the final wake-word release shape and ConfigMap contract, but activation is deferred until real model artifacts are present.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic model render and rollout-verification tooling</name>
  <read_first>
    - cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml
    - .planning/phases/02-satellite-1-voice-launch/02-RESEARCH.md
    - cluster/apps/house/home-assistant/home-assistant.yaml
  </read_first>
  <files>
    - cluster/apps/house/wyoming-openwakeword/models/MODEL.md
    - scripts/voice/render-wakeword-configmap.sh
    - scripts/voice/smoke-satellite1-runtime.sh
    - scripts/voice/check-wakeword-rollout.sh
  </files>
  <action>Create `models/MODEL.md` as the operator contract for externally generated artifacts with required fields `source_workflow_run`, `phrases`, `generated_at`, `sha256`, `approved_by`, and `notes` (per D-06). Add `scripts/voice/render-wakeword-configmap.sh` that fails fast unless both `cluster/apps/house/wyoming-openwakeword/models/ada.tflite` and `.../hey_ada.tflite` exist, computes SHA-256 values, and rewrites `wakeword-models.yaml` with `binaryData` plus checksum annotations. Add `scripts/voice/smoke-satellite1-runtime.sh` to verify the rendered ConfigMap, wake-word Service on `10400`, pod readiness, and model keys after deployment. Add `scripts/voice/check-wakeword-rollout.sh` to capture Home Assistant / Whisper / Piper pod identities, reconcile only the wake-word package, and prove model refreshes restart the wake-word deployment instead of the rest of the voice stack. Do not make these scripts depend on Music Assistant or any alternate HA conversation route (per D-03, D-07).</action>
  <verify>
    <automated>bash -n scripts/voice/render-wakeword-configmap.sh && bash -n scripts/voice/smoke-satellite1-runtime.sh && bash -n scripts/voice/check-wakeword-rollout.sh && grep -q "source_workflow_run" cluster/apps/house/wyoming-openwakeword/models/MODEL.md && grep -q "approved_by" cluster/apps/house/wyoming-openwakeword/models/MODEL.md</automated>
  </verify>
  <acceptance_criteria>
    - `render-wakeword-configmap.sh` validates artifact presence and rewrites `wakeword-models.yaml` deterministically.
    - `MODEL.md` captures provenance and approval metadata for both Ada phrases.
    - Runtime smoke coverage exists for service health, ConfigMap contents, and readiness.
    - Rollout coverage exists to prove WAKE-02 without redeploying Home Assistant, Whisper, or Piper.
  </acceptance_criteria>
  <done>Wake-word packaging and update handling are fully scripted and reviewable before any real model files are added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| external model workflow -> git repo | Untrusted binary artifacts enter the repo here |
| wake-word ConfigMap -> runtime pod | Mounted model payload controls live wake-word behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | T | `wakeword-models.yaml` | mitigate | Generate the ConfigMap only through `scripts/voice/render-wakeword-configmap.sh`, compute SHA-256 hashes, and record them in `models/MODEL.md` |
| T-02-02 | D | Home Assistant voice stack blast radius | mitigate | Keep the wake-word runtime in its own release and verify with `scripts/voice/check-wakeword-rollout.sh` that only the wake-word deployment rolls |
| T-02-03 | E | alternate HA conversation path | mitigate | Do not edit `home-assistant.yaml` or add any direct HA -> OpenClaw path in this plan; preserve the bridge route per D-07 |
</threat_model>

<verification>
- Static: `wyoming-openwakeword.yaml`, `wakeword-models.yaml`, and all new scripts pass the automated checks above.
- Operational tooling exists for runtime health and model-only rollout verification.
</verification>

<success_criteria>
- The repo contains a standalone wake-word release definition on port `10400`.
- Model artifacts can be rendered into Kubernetes manifests without hand-editing YAML.
- Rollout tooling exists to prove WAKE-02 before the release is activated.
</success_criteria>

<output>
After completion, create `.planning/phases/02-satellite-1-voice-launch/02-01-SUMMARY.md`
</output>
