---
phase: 02-satellite-1-voice-launch
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - cluster/apps/openclaw/instance/ha-config-snippet.yaml
  - .planning/phases/02-satellite-1-voice-launch/CUTOVER-CHECKLIST.md
  - .planning/phases/02-satellite-1-voice-launch/MANUAL-TEST.md
  - scripts/voice/smoke-satellite1-e2e.sh
  - scripts/voice/check-manual-fallback.sh
  - scripts/voice/check-pipeline-failure-response.sh
  - /config/configuration.yaml
  - /config/packages/ada_voice_failure.yaml
autonomous: false
requirements:
  - VOICE-01
  - VOICE-02
  - WAKE-01
  - WAKE-03
  - SAFE-01
user_setup:
  - service: satellite-1-device
    why: "This phase validates one physical known-good Satellite 1 endpoint."
    dashboard_config:
      - task: "Keep the chosen Satellite 1 powered on, reachable in Home Assistant, and available for spoken validation"
        location: "Physical device + Home Assistant Devices & Services"
must_haves:
  truths:
    - "Satellite 1 wakes Ada from the shipped `Ada` or `Hey Ada` model after the live device binding."
    - "Satellite 1 reaches Ada through the existing HA -> bridge -> OpenClaw -> Piper path and plays the reply back on the same device."
    - "Manual Assist still reaches Ada when the wake-word runtime is unavailable."
    - "When the voice pipeline fails, Satellite 1 plays a short explicit failure message instead of going silent."
  artifacts:
    - path: cluster/apps/openclaw/instance/ha-config-snippet.yaml
      provides: "Phase 2 operator instructions for Satellite 1, Wyoming wake word, fallback, bridge-health failure announcement, and bridge preservation"
    - path: .planning/phases/02-satellite-1-voice-launch/CUTOVER-CHECKLIST.md
      provides: "Live Satellite 1 setup steps for HA binding, custom wake-word selection, and bridge-health failure automation"
    - path: scripts/voice/check-manual-fallback.sh
      provides: "Automated WAKE-03 fallback verification"
    - path: scripts/voice/check-pipeline-failure-response.sh
      provides: "Automated SAFE-01 failure-path verification"
    - path: /config/packages/ada_voice_failure.yaml
      provides: "Home Assistant bridge-health sensor, announce script, and failure automation for Satellite 1"
  key_links:
    - from: cluster/apps/openclaw/instance/ha-config-snippet.yaml
      to: cluster/apps/openclaw/bridge/openai-proxy-ingress.yaml
      via: "bridge-backed OpenAI Conversation path"
      pattern: "api.openai.com|bridge"
    - from: scripts/voice/check-manual-fallback.sh
      to: cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml
      via: "wake-word runtime disable/restore test"
      pattern: "wyoming-openwakeword"
    - from: scripts/voice/check-pipeline-failure-response.sh
      to: assist_satellite.announce
      via: "bridge scaled down -> HA bridge-health automation -> spoken recovery message"
      pattern: "assist_satellite\\.announce"
    - from: /config/packages/ada_voice_failure.yaml
      to: cluster/apps/openclaw/bridge/bridge.yaml
      via: "REST health poll of `/healthz`"
      pattern: "openclaw-bridge\\.openclaw\\.svc\\.cluster\\.local:4000/healthz"
---

<objective>
Bind the live Satellite 1 device to Ada, keep manual Assist fallback explicit, and add a verified spoken failure path on the existing bridge-backed voice route.

Purpose: VOICE-01, VOICE-02, WAKE-01, WAKE-03, and SAFE-01 all depend on one concrete Satellite 1 cutover, not just manifests.
Output: Operator checklist, verification scripts, and a live-approved Satellite 1 voice path with fallback and failure behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-satellite-1-voice-launch/02-CONTEXT.md
@.planning/phases/02-satellite-1-voice-launch/02-RESEARCH.md
@.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-02-SUMMARY.md
@.planning/phases/01.1-ha-openclaw-shared-session-bridge/01.1-03-SUMMARY.md
@cluster/apps/openclaw/instance/ha-config-snippet.yaml
@cluster/apps/openclaw/bridge/bridge-runtime-configmap.yaml
@cluster/apps/house/home-assistant/home-assistant.yaml

<decisions>
D-01: Satellite 1 is one Assist-compatible device with both mic and speaker.
D-02: Replies return on the same Satellite 1 device.
D-03: Do not design this phase around split endpoints or multiple endpoint classes.
D-05: Manual Assist stays available as fallback.
D-07: Preserve the existing HA -> bridge -> OpenClaw -> Piper path.
D-08: Failures must be operator-visible, not silent.
</decisions>

<interfaces>
From `cluster/apps/house/home-assistant/home-assistant.yaml`:
```yaml
service:
  voice:
    controller: voice
    ports:
      whisper-wyoming:
        port: 10300
      piper-wyoming:
        port: 10200
```

From `cluster/apps/openclaw/instance/ha-config-snippet.yaml`:
```text
Home Assistant resolves api.openai.com through the internal hostAlias and bridge-owned ingress;
the Ada voice assistant stays on the OpenAI Conversation subentry instead of a custom YAML conversation backend.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Satellite 1 cutover docs and end-to-end verification scripts</name>
  <read_first>
    - .planning/phases/02-satellite-1-voice-launch/02-CONTEXT.md
    - .planning/phases/02-satellite-1-voice-launch/02-RESEARCH.md
    - cluster/apps/openclaw/instance/ha-config-snippet.yaml
    - cluster/apps/openclaw/bridge/bridge-runtime-configmap.yaml
  </read_first>
  <files>
    - cluster/apps/openclaw/instance/ha-config-snippet.yaml
    - .planning/phases/02-satellite-1-voice-launch/CUTOVER-CHECKLIST.md
    - .planning/phases/02-satellite-1-voice-launch/MANUAL-TEST.md
    - scripts/voice/smoke-satellite1-e2e.sh
    - scripts/voice/check-manual-fallback.sh
    - scripts/voice/check-pipeline-failure-response.sh
  </files>
  <action>Update `ha-config-snippet.yaml` so Phase 2 instructions explicitly preserve the current OpenAI Conversation bridge path (per D-07), register the wake-word service at `wyoming-openwakeword.house:10400`, keep Whisper on `10300`, keep Piper on `10200`, and bind one chosen `assist_satellite` entity as both input and output (per D-01, D-02, D-03). Create `CUTOVER-CHECKLIST.md` with exact live steps to: record the chosen `assist_satellite.<entity_id>` as `SATELLITE_ENTITY_ID`; bind Ada to that device; select the shipped `Ada` and `Hey Ada` wake-word models on that same device to satisfy WAKE-01; leave the manual Assist trigger enabled; ensure `/config/configuration.yaml` includes `homeassistant: packages: !include_dir_named packages`; and create `/config/packages/ada_voice_failure.yaml` containing (1) `binary_sensor.ada_voice_bridge_healthy` using `platform: rest` against `http://openclaw-bridge.openclaw.svc.cluster.local:4000/healthz`, (2) `script.ada_voice_pipeline_failure_announce` that calls `assist_satellite.announce` on `SATELLITE_ENTITY_ID`, and (3) an automation that triggers when the sensor is `off` or `unavailable` for `00:00:15` and announces `Ada isn't available right now. Hold the Assist button and try again in a moment.` (per D-05, D-08). Create `MANUAL-TEST.md` plus three scripts: `smoke-satellite1-e2e.sh` for `Ada` / `Hey Ada` wake-word plus bridge-path and same-device routing checks, `check-manual-fallback.sh` for WAKE-03 by disabling or scaling down the wake-word runtime, and `check-pipeline-failure-response.sh` for SAFE-01 by scaling `deployment/openclaw-bridge` in namespace `openclaw` to `0`, waiting for the HA bridge-health automation to fire, and restoring the deployment. Do not add Music Assistant routing, a second device class, or any plan step that depends on an undocumented Assist pipeline event-bus trigger.</action>
  <verify>
    <automated>bash -n scripts/voice/smoke-satellite1-e2e.sh && bash -n scripts/voice/check-manual-fallback.sh && bash -n scripts/voice/check-pipeline-failure-response.sh && grep -q "wyoming-openwakeword.house:10400" cluster/apps/openclaw/instance/ha-config-snippet.yaml && grep -q "packages: !include_dir_named packages" .planning/phases/02-satellite-1-voice-launch/CUTOVER-CHECKLIST.md && grep -q "openclaw-bridge.openclaw.svc.cluster.local:4000/healthz" .planning/phases/02-satellite-1-voice-launch/CUTOVER-CHECKLIST.md && grep -q "assist_satellite.announce" .planning/phases/02-satellite-1-voice-launch/CUTOVER-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - Operator docs keep the bridge-backed conversation path and same-device reply path explicit.
    - Operator docs name the exact WAKE-01 binding outcome on one Satellite 1 device using `Ada` / `Hey Ada`.
    - The cutover checklist includes the exact failure announcement text and the manual Assist fallback requirement.
    - Automated scripts exist for wake-word plus happy-path voice, wake-word outage fallback, and forced bridge-failure behavior.
  </acceptance_criteria>
  <done>The repo now contains exact Phase 2 cutover and verification instructions instead of relying on ad-hoc live debugging.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Bind the real Satellite 1 device and install fallback/failure behavior in Home Assistant</name>
  <read_first>
    - .planning/phases/02-satellite-1-voice-launch/CUTOVER-CHECKLIST.md
    - .planning/phases/02-satellite-1-voice-launch/MANUAL-TEST.md
    - cluster/apps/openclaw/instance/ha-config-snippet.yaml
    - scripts/voice/smoke-satellite1-e2e.sh
    - scripts/voice/check-manual-fallback.sh
    - scripts/voice/check-pipeline-failure-response.sh
  </read_first>
  <files>
    - .planning/phases/02-satellite-1-voice-launch/CUTOVER-CHECKLIST.md
    - /config/.storage/core.config_entries
    - /config/.storage/assist_pipeline.pipelines
    - /config/configuration.yaml
    - /config/packages/ada_voice_failure.yaml
  </files>
  <action>Follow `CUTOVER-CHECKLIST.md` end to end: choose the known-good Satellite 1 `assist_satellite` entity, bind Ada to that one device as both microphone and speaker, register the Wyoming wake-word service, select the shipped custom `Ada` / `Hey Ada` models on that device, keep manual Assist invocation enabled as the fallback path, ensure `/config/configuration.yaml` includes the packages directory, and create `/config/packages/ada_voice_failure.yaml` with the exact REST sensor + announce script + automation specified in the checklist. After the live config is saved and Home Assistant reloads packages/automations, run `scripts/voice/smoke-satellite1-e2e.sh`, `scripts/voice/check-manual-fallback.sh`, and `scripts/voice/check-pipeline-failure-response.sh`. Do not reroute replies through Music Assistant, do not change the HA conversation backend away from the existing bridge path, and do not replace the bridge-health mechanism with a nonexistent Assist pipeline event trigger.</action>
  <what-built>Plans 02-01 and 02-02 delivered the wake-word runtime and custom model package. This checkpoint binds them to the real Satellite 1 device, completes WAKE-01 on live hardware, and installs the concrete bridge-health failure path the user will actually hear.</what-built>
  <verify>
    <automated>grep -q "!include_dir_named packages" /config/configuration.yaml && grep -q "ada_voice_bridge_healthy" /config/packages/ada_voice_failure.yaml && grep -q "openclaw-bridge.openclaw.svc.cluster.local:4000/healthz" /config/packages/ada_voice_failure.yaml && grep -q "assist_satellite.announce" /config/packages/ada_voice_failure.yaml && bash scripts/voice/smoke-satellite1-e2e.sh && bash scripts/voice/check-manual-fallback.sh && bash scripts/voice/check-pipeline-failure-response.sh</automated>
  </verify>
  <how-to-verify>
1. Open Home Assistant and select the single known-good Satellite 1 device.
2. Bind the existing Ada voice assistant to that device.
3. Register/select the Wyoming wake-word service on `wyoming-openwakeword.house:10400`.
4. Choose the custom `Ada` / `Hey Ada` wake-word models for that device.
5. Leave push-to-talk/manual Assist enabled.
 6. Ensure `/config/configuration.yaml` includes `homeassistant: packages: !include_dir_named packages`.
 7. Create `/config/packages/ada_voice_failure.yaml` exactly as documented so `binary_sensor.ada_voice_bridge_healthy` polls the bridge `/healthz` endpoint and the matching automation calls `assist_satellite.announce` on the same Satellite 1 entity after 15 seconds of bridge failure.
 8. Reload packages/automations or restart Home Assistant.
 9. Run the three scripts above and confirm they pass.
  </how-to-verify>
  <resume-signal>Type `satellite bound` when the device is live in Home Assistant and all three scripts pass, or describe the failure.</resume-signal>
  <acceptance_criteria>
    - One concrete Satellite 1 device is bound to Ada as both input and output.
    - The custom Ada wake-word path is selected on that device and wakes on `Ada` or `Hey Ada`.
    - Manual Assist remains enabled and passes the scripted fallback check.
    - The spoken SAFE-01 bridge-health failure path is installed in `/config/packages/ada_voice_failure.yaml` and passes the scripted failure check.
  </acceptance_criteria>
  <done>The live Home Assistant setup now matches the Phase 2 contract for one known-good Satellite 1 device.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve spoken wake-word, fallback, and failure behavior on the physical device</name>
  <read_first>
    - .planning/phases/02-satellite-1-voice-launch/MANUAL-TEST.md
    - scripts/voice/smoke-satellite1-e2e.sh
    - scripts/voice/check-manual-fallback.sh
    - scripts/voice/check-pipeline-failure-response.sh
  </read_first>
  <files>
    - .planning/phases/02-satellite-1-voice-launch/MANUAL-TEST.md
    - scripts/voice/smoke-satellite1-e2e.sh
    - scripts/voice/check-manual-fallback.sh
    - scripts/voice/check-pipeline-failure-response.sh
  </files>
  <action>Use the physical Satellite 1 device to approve the final user-visible behavior: `Ada` / `Hey Ada` wake-word activation first, same-device response routing second, manual Assist fallback third, and explicit spoken bridge-failure feedback fourth.</action>
  <what-built>Full Phase 2 launch behavior on one known-good Satellite 1 device, still using the existing bridge-backed Ada path.</what-built>
  <verify>
    <automated>bash scripts/voice/smoke-satellite1-e2e.sh && bash scripts/voice/check-manual-fallback.sh && bash scripts/voice/check-pipeline-failure-response.sh</automated>
  </verify>
  <how-to-verify>
1. Say `Ada` and `Hey Ada` near Satellite 1. Ask a short question such as `What is your name?`
   - Expected: Ada replies through the same Satellite 1 speaker.
2. Stop or disable the wake-word runtime as directed by `MANUAL-TEST.md`, then use the manual Assist trigger on Satellite 1.
   - Expected: Ada still answers through the same device.
3. Run the forced-failure scenario from `MANUAL-TEST.md`.
   - Expected: Satellite 1 speaks `Ada isn't available right now. Hold the Assist button and try again in a moment.` instead of silence.
4. Re-enable the wake-word runtime and repeat one successful `Ada` wake.
  </how-to-verify>
  <resume-signal>Type `approved` if all spoken behaviors match, or describe the mismatch.</resume-signal>
  <acceptance_criteria>
    - VOICE-01: Satellite 1 reaches Ada and gets a spoken response.
    - VOICE-02: The response plays on the initiating Satellite 1 device.
    - WAKE-01: Satellite 1 wakes on the shipped `Ada` or `Hey Ada` model.
    - WAKE-03: Manual Assist still works when wake word is unavailable.
    - SAFE-01: The user hears the explicit failure message on Satellite 1 when the pipeline cannot complete.
  </acceptance_criteria>
  <done>Phase 2 is user-verified on the physical device for wake-word, fallback, and failure behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Satellite 1 user speech -> Home Assistant voice pipeline | Untrusted live audio enters the system here |
| bridge health sensor -> spoken recovery message | Failure handling must not silently drop the user response |
| Satellite 1 reply target -> physical room audio | Wrong-target playback leaks output to the wrong place |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-07 | I | Satellite reply target | mitigate | Bind one concrete Satellite 1 device as both input and output and verify same-device playback with `scripts/voice/smoke-satellite1-e2e.sh` |
| T-02-08 | D | wake-word outage handling | mitigate | Script and manually verify push-to-talk/manual Assist fallback with `scripts/voice/check-manual-fallback.sh` |
| T-02-09 | R | pipeline failure UX | mitigate | Install `/config/packages/ada_voice_failure.yaml` with `binary_sensor.ada_voice_bridge_healthy`, the `assist_satellite.announce` script, and the 15-second bridge-failure automation; verify it with `scripts/voice/check-pipeline-failure-response.sh` |
| T-02-10 | S/E | alternate HA backend path | mitigate | Keep the bridge-backed OpenAI Conversation route documented and unchanged in `ha-config-snippet.yaml`; do not add a direct OpenClaw endpoint |
</threat_model>

<verification>
- Static: new docs and scripts exist and pass shell syntax checks.
- Live: all three scripts pass after the Satellite 1 cutover.
- Human: spoken UAT on the physical device is approved.
</verification>

<success_criteria>
- Satellite 1 wakes on the shipped `Ada` / `Hey Ada` models, uses the existing HA -> bridge -> OpenClaw -> Piper path, and replies on the same device.
- Manual Assist fallback remains available when wake word is unavailable.
- SAFE-01 failure handling is explicit and spoken on Satellite 1 through the documented bridge-health automation package.
</success_criteria>

<output>
After completion, create `.planning/phases/02-satellite-1-voice-launch/02-03-SUMMARY.md`
</output>
