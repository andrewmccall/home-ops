---
phase: 02-satellite-1-voice-launch
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - cluster/apps/house/kustomization.yaml
  - cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml
  - cluster/apps/house/wyoming-openwakeword/models/ada.tflite
  - cluster/apps/house/wyoming-openwakeword/models/hey_ada.tflite
  - cluster/apps/house/wyoming-openwakeword/models/MODEL.md
autonomous: false
requirements:
  - WAKE-02
user_setup:
  - service: external-wakeword-artifacts
    why: "The Ada / Hey Ada `.tflite` models are generated outside this repo per the locked phase decision."
    dashboard_config:
      - task: "Export the approved `ada.tflite` and `hey_ada.tflite` files and the source workflow reference"
        location: "External wake-word training/generation workflow output"
must_haves:
  truths:
    - "The repo contains approved `Ada` and `Hey Ada` wake-word artifacts with recorded provenance and ready for Satellite 1 binding."
    - "The `house` kustomization activates the dedicated wake-word release only after the real model artifacts are present."
    - "An operator can refresh the models and redeploy only the wake-word package."
  artifacts:
    - path: cluster/apps/house/wyoming-openwakeword/models/ada.tflite
      provides: "Primary custom Ada wake-word model"
    - path: cluster/apps/house/wyoming-openwakeword/models/hey_ada.tflite
      provides: "Secondary custom Hey Ada wake-word model"
    - path: cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml
      provides: "Rendered ConfigMap carrying both model binaries"
    - path: cluster/apps/house/kustomization.yaml
      provides: "Activation of the dedicated wake-word release"
  key_links:
    - from: cluster/apps/house/wyoming-openwakeword/models/ada.tflite
      to: cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml
      via: "render-wakeword-configmap.sh output"
      pattern: "ada.tflite"
    - from: cluster/apps/house/kustomization.yaml
      to: cluster/apps/house/wyoming-openwakeword/kustomization.yaml
      via: "resource include"
      pattern: "wyoming-openwakeword"
---

<objective>
Bring the real Ada wake-word artifacts into the repo, activate the dedicated wake-word release, and prove model-only deployment/update behavior.

Purpose: WAKE-02 is only complete when the real `Ada` / `Hey Ada` models are mounted in-cluster and operators can refresh them without replacing the rest of the voice path; the user-visible WAKE-01 bind/approval happens in 02-03.
Output: Real model artifacts, rendered ConfigMap, activated house kustomization entry, and a verified wake-word rollout.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-satellite-1-voice-launch/02-CONTEXT.md
@.planning/phases/02-satellite-1-voice-launch/02-RESEARCH.md
@.planning/phases/02-satellite-1-voice-launch/02-01-PLAN.md
@cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml
@cluster/apps/house/kustomization.yaml

<decisions>
D-04: Custom "Ada" / "Hey Ada" wake word is the primary launch path.
D-06: Model training stays external; packaging, deployment, and updates happen in-repo and in-cluster.
D-07: Preserve the existing HA -> bridge -> OpenClaw -> Piper path.
</decisions>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Supply the approved external Ada wake-word artifacts</name>
  <read_first>
    - cluster/apps/house/wyoming-openwakeword/models/MODEL.md
    - scripts/voice/render-wakeword-configmap.sh
    - .planning/phases/02-satellite-1-voice-launch/02-RESEARCH.md
  </read_first>
  <files>
    - cluster/apps/house/wyoming-openwakeword/models/ada.tflite
    - cluster/apps/house/wyoming-openwakeword/models/hey_ada.tflite
    - cluster/apps/house/wyoming-openwakeword/models/MODEL.md
  </files>
  <action>Obtain the externally generated `ada.tflite` and `hey_ada.tflite` artifacts from the approved wake-word workflow (per D-06), place them at the exact repo paths above, and fill `models/MODEL.md` with the source workflow reference, generation time, phrases, and approver before resuming. Do not rename the files, do not substitute built-in wake words, and do not skip one of the two phrases (per D-04).</action>
  <what-built>Plan 02-01 prepared the dedicated release, render script, and metadata contract. This checkpoint provides the real external artifacts that release will ship.</what-built>
  <verify>
    <automated>test -s cluster/apps/house/wyoming-openwakeword/models/ada.tflite && test -s cluster/apps/house/wyoming-openwakeword/models/hey_ada.tflite && shasum -a 256 cluster/apps/house/wyoming-openwakeword/models/ada.tflite cluster/apps/house/wyoming-openwakeword/models/hey_ada.tflite && grep -q "source_workflow_run" cluster/apps/house/wyoming-openwakeword/models/MODEL.md</automated>
  </verify>
  <how-to-verify>
1. Export the approved `ada.tflite` and `hey_ada.tflite` files from the external workflow.
2. Save them to:
   - `cluster/apps/house/wyoming-openwakeword/models/ada.tflite`
   - `cluster/apps/house/wyoming-openwakeword/models/hey_ada.tflite`
3. Update `cluster/apps/house/wyoming-openwakeword/models/MODEL.md` with:
   - source workflow run or URL
   - generated timestamp
   - phrases covered
   - approver
4. Run:
   ```bash
   shasum -a 256 cluster/apps/house/wyoming-openwakeword/models/ada.tflite cluster/apps/house/wyoming-openwakeword/models/hey_ada.tflite
   ```
5. Resume once both files exist and the metadata is filled in.
  </how-to-verify>
  <resume-signal>Type `artifacts ready` once both `.tflite` files exist at the required paths and `MODEL.md` is filled in.</resume-signal>
  <acceptance_criteria>
    - Both `.tflite` files exist at the exact planned paths and are non-empty.
    - `MODEL.md` records the external source workflow and approval context.
    - SHA-256 hashes can be computed for both artifacts before deployment.
  </acceptance_criteria>
  <done>The real Ada wake-word artifacts are available in-repo and ready to render into the Kubernetes ConfigMap and bind to Satellite 1 in 02-03.</done>
</task>

<task type="auto">
  <name>Task 2: Render, activate, and verify the wake-word release</name>
  <read_first>
    - .planning/phases/02-satellite-1-voice-launch/02-01-PLAN.md
    - cluster/apps/house/wyoming-openwakeword/wyoming-openwakeword.yaml
    - scripts/voice/render-wakeword-configmap.sh
    - scripts/voice/smoke-satellite1-runtime.sh
    - scripts/voice/check-wakeword-rollout.sh
  </read_first>
  <files>
    - cluster/apps/house/kustomization.yaml
    - cluster/apps/house/wyoming-openwakeword/wakeword-models.yaml
    - cluster/apps/house/wyoming-openwakeword/models/MODEL.md
  </files>
  <action>Run `scripts/voice/render-wakeword-configmap.sh` to rewrite `wakeword-models.yaml` from the real `.tflite` artifacts and backfill the computed hashes into `models/MODEL.md`. Then add `- wyoming-openwakeword` to `cluster/apps/house/kustomization.yaml`, reconcile the `house` kustomization (or apply the kustomization directly if Flux remains unhealthy), and run `scripts/voice/smoke-satellite1-runtime.sh`. After the initial rollout is healthy, re-run the render step once more and use `scripts/voice/check-wakeword-rollout.sh` to prove that a model refresh changes only the wake-word release. Do not edit `cluster/apps/house/home-assistant/home-assistant.yaml`, Music Assistant manifests, or the bridge/OpenClaw route (per D-07).</action>
  <verify>
    <automated>grep -q "wyoming-openwakeword" cluster/apps/house/kustomization.yaml && bash scripts/voice/render-wakeword-configmap.sh && bash scripts/voice/smoke-satellite1-runtime.sh && bash scripts/voice/check-wakeword-rollout.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cluster/apps/house/kustomization.yaml` activates the wake-word release only after real artifacts are present.
    - `wakeword-models.yaml` is rendered from the `.tflite` files, not hand-edited.
    - Runtime smoke checks pass against the live `10400` Wyoming service.
    - Rollout checks prove model refreshes do not redeploy Home Assistant, Whisper, or Piper.
  </acceptance_criteria>
  <done>The custom Ada wake-word release is live in-cluster and operators can refresh the model package independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| external model artifacts -> repo paths | Binary artifacts cross into source control here |
| activated house kustomization -> cluster | This step decides whether wake-word code becomes live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-04 | T | external `.tflite` artifacts | mitigate | Require exact filenames, compute SHA-256 hashes, and record provenance in `models/MODEL.md` before activation |
| T-02-05 | D | `cluster/apps/house/kustomization.yaml` | mitigate | Add the wake-word resource only after the real artifacts exist and the ConfigMap is rendered successfully |
| T-02-06 | R | model-update blast radius | mitigate | Use `scripts/voice/check-wakeword-rollout.sh` to prove only the wake-word release rolls during updates |
</threat_model>

<verification>
- Human checkpoint confirms the real `ada.tflite` and `hey_ada.tflite` files are present.
- Automated render, runtime smoke, and rollout checks all pass.
</verification>

<success_criteria>
- Both custom wake-word artifacts are committed at the planned paths with provenance metadata and are ready for Satellite 1 binding.
- The wake-word release is activated under `cluster/apps/house/`.
- Model refresh verification proves WAKE-02 without touching the rest of the voice stack.
</success_criteria>

<output>
After completion, create `.planning/phases/02-satellite-1-voice-launch/02-02-SUMMARY.md`
</output>
